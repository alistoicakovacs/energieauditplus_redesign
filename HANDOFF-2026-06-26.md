# Handoff — EA+ Design System Revamp (resume)

**Date:** 2026-06-26 · **Continuing:** next session
**Figma file:** EnergieAudit Plus — Design System · `fileKey 9LNbES4qpDoHgOmBxzIoWP`

## Goal
A genuine premium revamp (not minor tweaks) of the Figma design system + switch typography to **Calibri**. User's north star: bold, modern, clean, award-winning, premium. Approved visual directions were locked via browser mockups before building.

## Source-of-truth artifacts (read these first — don't duplicate)
- **Design spec:** `docs/superpowers/specs/2026-06-26-component-revamp-design.md` — locked decisions, tokens, per-component anatomy, Reactbits map.
- **Auto-memory:** `C:\Users\alist\.claude\projects\C--Users-alist-desktop-website-redesign\memory\figma-design-system.md` (+ `figma-build-gotchas.md`) — full revamp log with node IDs; loaded automatically next session.
- **Brand foundations:** `handoff/02-style-guide.md` (colors, type strategy). Content per page in `handoff/content/*.md`.
- **Approved mockups (HTML):** `.superpowers/brainstorm/3731-1782475316/content/` — `team-card-B-white.html`, `header-directions.html` (D2), `button-family.html`. The companion server is likely stopped; just open the files.

## Done & verified this session
1. **Fonts → Calibri (Carlito):** flipped 6 heading text styles (Display, Heading/H1–H4, Stat) from Caladea→Carlito; then a **file-wide sweep** converted all remaining raw Caladea + 2 stray real-Calibri text nodes to Carlito. Re-scan = 0 non-Carlito remaining.
2. **Buttons** (`COMPONENT_SET 12:81`): all 15 variants → pill (bound to `radius/pill`). Star Border dropped.
3. **Card / Team** (`COMPONENT 17:19`): rebuilt = Direction-B-on-white (gradient accent, diagonal-cut photo, glass location pill, centered Calibri name + green rule + role overline + credentials, mail+phone icon buttons, Termin CTA).
4. **Header / Desktop** (`COMPONENT 24:6`): rebuilt = two-tier (navy utility strip + white main bar, gradient logo mark + green spark, nav w/ Leistungen chevron, green WhatsApp btn, CTA).
5. **Leistungen Mega-Menu** (`FRAME 44:15` on Header page): 7 service tiles (bespoke straight-line icons in tinted chips) + dark "Alle Leistungen" tile.
6. **Icon/whatsapp** (`COMPONENT 41:2`) added to Icons page.
7. **Card / Reference** (`COMPONENT 17:6`): rebuilt to new card language (accent, diagonal image, location pill, Calibri title, green rule, QNG/LCA/Blower-Door tinted chips, "Projekt ansehen →" link).
8. **Footer** (`COMPONENT 25:6`): brand column (`25:9`) rebuilt — new gradient logo + Calibri two-tone wordmark (white + green-400 "Plus"), tagline, phone/email/address, mail/phone/whatsapp circular icon buttons. Existing 5-col dark structure kept.

## Not yet done (next session backlog)
- **Header / Mobile** (`COMPONENT 24:26`) — apply two-tier/hamburger + fixed CTA to match desktop.
- **Card / Service** (`COMPONENT 16:6`) and **Card / Stat** (`COMPONENT 16:18`) — still old plain style; bring to new card language (accent bar, green rule, Calibri, icon chips).
- **CTA Band** (page `26:2`) — align to new style.
- Optional review: Badge (`14:2`), Form Input (`15:2`), Link & Overline (`13:2`) for consistency.
- Not started (from prior scope): Code Connect, publish as shared Team Library, full §5 component set.

## Key facts & gotchas for resuming
- **Inspect with `use_figma`, not `get_metadata`** for page discovery — `get_metadata` with no nodeId misleadingly returned only "Cover"; the file actually has ~13 pages. Variable/style/component IDs are listed in the memory file & spec.
- Fonts available in this Figma: **Carlito + Caladea only** (Regular/Bold/Italic/Bold Italic). Real **Calibri is NOT installed** → always use Carlito as the Figma stand-in. Code stack = `"Calibri","Carlito",system-ui,…`.
- **`loadAllPagesAsync` is unsupported** here (non-dynamic doc) — pages are already loaded, so iterate `figma.root.children` directly (no `setCurrentPageAsync` needed for cross-page reads/edits).
- **Text FILL bug:** setting `layoutSizingHorizontal='FILL'` on auto-height TEXT collapsed width to 0. Workaround used: fixed width = container inner width (e.g. 296) + `textAutoResize='HEIGHT'`.
- Other gotchas (resize resets sizing modes; bind tinted fills to **primitive** vars; `use_figma` atomic) are in `figma-build-gotchas.md`.
- Diagonal photo "cut" = a white `createVector` triangle overlay inside a `clipsContent` image frame.
- Reactbits map (for eventual Next.js build): Spotlight Card (card hover), Tilted Card, animated gradient nav underline, Magnet/Click Spark on CTA. Star Border removed per user.

## Suggested skills for next session
- **`figma-use`** + **`figma-generate-library`** — MANDATORY before any `use_figma` call. Load via `ReadMcpResourceTool` (`skill://figma/figma-use/SKILL.md`, `…/figma-generate-library/SKILL.md`) and pass `skillNames:"resource:figma-use,resource:figma-generate-library"` on every `use_figma`.
- **`brainstorming`** — only if a *new* direction is needed; the card/header/button/footer language is already locked, so remaining components (Service/Stat cards, mobile header, CTA band) are execution against the established system, not new design.

## Resume prompt suggestion
> "Continue the EA+ Figma revamp (file 9LNbES4qpDoHgOmBxzIoWP). Read HANDOFF-2026-06-26.md and the spec. Next: revamp Card/Service (16:6) and Card/Stat (16:18) to the new card language, then Header/Mobile (24:26)."
